{
  "name": "phone-network-server",
  "version": "0.1.0",
  "description": "Phone Network — Control server",
  "main": "dist/phone-network-server/src/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && mkdir -p dist/phone-network-server/src/modules/skills/templates && cp -r src/modules/skills/templates/. dist/phone-network-server/src/modules/skills/templates/ && mkdir -p dist/modules/skills/templates && cp -r src/modules/skills/templates/. dist/modules/skills/templates/ && mkdir -p dist/modules/skills/fixed-elements && cp -r src/modules/skills/fixed-elements/. dist/modules/skills/fixed-elements/ && mkdir -p dist/phone-network-server/src/modules/skills/fixed-elements && cp -r src/modules/skills/fixed-elements/. dist/phone-network-server/src/modules/skills/fixed-elements/ && mkdir -p dist/src/modules/skills/fixed-elements && cp -r src/modules/skills/fixed-elements/. dist/src/modules/skills/fixed-elements/",
    "start": "node dist/phone-network-server/src/index.js",
    "db:migrate": "node -r tsx/cjs src/db/migrate.ts",
    "db:seed": "node -r tsx/cjs src/db/seed.ts",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@types/multer": "^2.1.0",
    "bullmq": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "image-size": "^2.0.2",
    "ioredis": "^5.3.0",
    "multer": "^2.1.1",
    "pg": "^8.11.0",
    "prom-client": "^15.1.0",
    "uuid": "^9.0.0",
    "ws": "^8.14.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.0",
    "@types/uuid": "^9.0.0",
    "@types/ws": "^8.5.0",
    "eslint": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
